import { MDSSelectContainerProps } from './@types';

// Ticket:
export const MDSSelectContainer = ({ title = undefined, ...props }: MDSSelectContainerProps) => {
  return <div>Add logic here</div>;
};
