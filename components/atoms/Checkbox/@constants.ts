import { Theme } from './@types';

export const THEME: Theme = {
  size: {
    small: {
      boxSize: 20,
      padding: 6,
      borderRadius: 8,
    },
    medium: {
      boxSize: 24,
      padding: 4,
      borderRadius: 8,
    },
  },
};
